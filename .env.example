# 日志配置
# 日志级别：trace, debug, info, warn, error, fatal
# 开发环境建议使用 debug，生产环境建议使用 info
LOG_LEVEL=debug

# 是否使用美化日志输出（仅开发环境）
# Windows 控制台默认不启用（避免中文乱码），设置为 true 可强制启用
# 如果要在 Windows 上查看美化日志，建议：
# 1. 使用 Windows Terminal 并设置为 UTF-8
# 2. 或在 PowerShell 中运行：[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
# USE_PRETTY_LOG=true
